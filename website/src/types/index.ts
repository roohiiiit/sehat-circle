// Shared TypeScript types for Sehat Circle landing page

export interface ComparisonRowData {
  feature: string;
  traditional: string;
  sehatCircle: string;
}

export type PhoneMockupVariant = 'manager' | 'elder';
